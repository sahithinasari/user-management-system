pipeline {

    agent any

    stages {

        stage('Checkout') {
            steps {
                echo 'checkout..'
            }
        }

        stage('Test') {
            steps {
                echo 'testing..'
            }
        }

        stage('Docker Build') {
            steps {
                echo 'docker build ...'
            }
        }
    }
}
